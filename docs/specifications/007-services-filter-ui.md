# Спецификация 007: фильтр каталога в HTML

Корневая страница принимает необязательный параметр `category` со значением
`women` или `men`. Без параметра отображаются все активные услуги. Неактивные
услуги не отображаются ни в общем каталоге, ни в фильтре.
